services:
  tracker:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - STATE_FILE=/data/state.json
    volumes:
      - tracker_data:/data

volumes:
  tracker_data:
